version: '3.8'

services:
  web:
    build: .
    container_name: telegram-bot
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - TELEGRAM_DEBUG=true
    restart: unless-stopped
